import { html, LitElement } from 'lit';
import { property } from 'lit/decorators.js';
import styles from './{{kebabCase name}}.styles.js';

/**
 * Add a description here
 *
 * @tag my-{{kebabCase name}}
 * @since 0.0.0
 * @status experimental
 *
 **/
export class My{{pascalCase name}} extends LitElement {
  static override styles = styles;

  @property() 
  heading = 'Hello, word!';

  override render() {
    return html`
      <h1>${this.heading}</h1>
    `;
  }
}

export default My{{pascalCase name}};
